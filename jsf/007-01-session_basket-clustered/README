In this use case (007) and stage (01) I aim to expand on the
previous stage (00) so as to build the clustered version of the
basket. I hope that I only need to add the <distributable> tag
in web.xml.
